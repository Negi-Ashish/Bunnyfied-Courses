import CheckboxComponent from "../components/CheckboxComponent";

const component = () => {
  return (
    <div>
      {/* Other page content */}
      <CheckboxComponent />
      {/* Other page content */}
    </div>
  );
};

export default component;
